#include <iostream>

#include "src/observer.h"

using namespace std::chrono;

int main(int argc, char *argv[]) {
    Observer observer;
    // Sender sender;
    // Receiver receiver;
    // receiver.start();
    // while (true) {
        
    // }
    // receiver.stop();
}
